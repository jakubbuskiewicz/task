{
  "name": "@vandeley/backend",
  "version": "1.0.0",
  "main": "src/index.ts",
  "type": "commonjs",
  "scripts": {
    "dev": "nodemon -q --watch src --ext ts --exec \"pnpm tsx src/index.ts\"",
    "build": "tsc -p tsconfig.json",
    "start": "node bin/index.js",
    "lint": "eslint . --config ../eslint.config.mjs",
    "format": "prettier --write ."
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-fetch": "^2.6.13",
    "nodemon": "^3.1.10",
    "typescript": "^5.0.0"
  }
}
